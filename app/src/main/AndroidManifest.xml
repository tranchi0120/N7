<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nhom7">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icons8shopee"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icons8shopee"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".singup" />
        <activity android:name=".OutActivity" />
        <activity android:name=".QrActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LoadingLoginActivity" />
        <activity android:name=".GridItemActivity" />
        <activity android:name=".UpcomingActivity" />
        <activity android:name=".NewActivity" />
        <activity android:name=".History" />
        <activity
            android:name=".singup"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".Geography" />
        <activity android:name=".LoginMainActivity" />
        <activity android:name=".MainAction" />
        <activity android:name=".IdolActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".Home" />
        <activity android:name=".Notification" />
        <activity android:name=".Live" />
        <activity android:name=".Feedback" />
        <activity android:name=".Activity_cn" />
        <activity android:name=".Activity_Hd" />
        <activity android:name=".Activity_Shopee" />
        <activity android:name=".Activity_Now" />
        <activity
            android:name=".QrActivity"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".Activity_Sale" />
        <activity android:name=".BasketActivity" />
        <activity android:name=".MessengerActivity" />
        <activity android:name=".product.Activity_dg" />
        <activity android:name=".product.Myaccount" />
        <activity android:name=".product.Activity_vi" />
        <activity android:name=".product.Activity_via" />
        <activity android:name=".product.Activity_voucher" />
        <activity android:name=".product.Activity_xu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts"
            android:value="AIzaSyDaujFyoeH8NsgrXGQc_OrEAX1k4sw7ClM" />
    </application>

</manifest>